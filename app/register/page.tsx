"use client";
import { FormEvent, useState } from "react";

export default function Register(){
 const [loading,setLoading]=useState(false),[msg,setMsg]=useState(""),[ok,setOk]=useState(false);
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMsg("");setOk(false);const form=new FormData(e.currentTarget);const payload=Object.fromEntries(form.entries());
  if(payload.password!==payload.confirmPassword){setMsg("Passwords do not match.");setLoading(false);return;}
  try{const res=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await res.json();if(!res.ok)throw new Error(data.error||"Registration failed.");setOk(true);setMsg("Account created successfully. Check your email if verification is enabled.");e.currentTarget.reset();}catch(err:any){setMsg(err.message||"Registration failed.");}finally{setLoading(false)}}
 return <main className="subpage"><div className="container"><a className="back-link" href="/">← Back to Institutional Trading Academy</a><section className="register-card"><div className="eyebrow">JOIN THE ACADEMY</div><h1>Create your account.</h1><p>Get educational resources, academy updates and future offers directly from ITA.</p><form onSubmit={submit}><label>Full name<input name="name" required minLength={2} placeholder="Your name"/></label><label>Email address<input type="email" name="email" required placeholder="you@example.com"/></label><label>Phone number <span className="optional">(optional)</span><input type="tel" name="phone" placeholder="+237 ..."/></label><label>Password<input type="password" name="password" required minLength={8} placeholder="Minimum 8 characters"/></label><label>Confirm password<input type="password" name="confirmPassword" required minLength={8} placeholder="Repeat your password"/></label><label className="consent"><input type="checkbox" name="marketingConsent" value="true" required/> <span>I agree to receive Institutional Trading Academy educational and marketing communications. I understand I can unsubscribe later.</span></label><button className="button primary full" disabled={loading}>{loading?"Creating account…":"Create Account"}</button></form>{msg&&<p className={ok?"form-message":"form-error"}>{msg}</p>}<p className="privacy-note">Your password is never stored in plain text. Contact information is used only according to your consent and the academy's privacy policy.</p></section></div></main>}